# This folder contains all the models that I have trained using my program.

# This folder's content (models.zip) in this folder has to be unzip and the models' files (in the H5 and KERAS file types) has to be brought up to this folder (models) so that the folder's content is properly read by the program and be used for image classification.

# The model.zip folder (models/models.zip) and the empty drums folder (models/models) can be deleted after doing the step above to avoid confusing the program.

# Remember to delete this ReadMe.txt as well.
